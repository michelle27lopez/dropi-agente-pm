.DS_Store
.claude/*
!.claude/skills/
.claude/skills/*
!.claude/skills/sync-diario/
!.claude/skills/bug-jira/
.mcp.json
.impeccable/

# Cerebro privado (repo anidado propio: cerebro-logistica-dropi).
# Vive dentro de esta carpeta pero NUNCA se sube al repo del equipo.
/cerebro/

# Data del pipeline de recolecciones. `datos-recolecciones.json` SÍ se versiona:
# el prototipo lo necesita para renderizar en Vercel (si no, 404). `centroides.json`
# es intermedio de build (no runtime) y se queda local.
hub/public/logistica/recolecciones/pipeline/centroides.json
